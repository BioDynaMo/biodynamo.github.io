'use strict';

module.exports.getAllAttributes = function (options) {
    return Object.keys(options.hash).map(key => `${key}="${options.hash[key]}"`).join(' ');
};

module.exports.parseVal = function (val) {
    return val !== undefined ? val : '';
};
