'use strict';

const hbs = require('express-hbs');
const boxStyles = require('./styles/box');

hbs.registerHelper('gh-box', function (style) {
    let cssClass = boxStyles(style);

    return new hbs.handlebars.SafeString(cssClass);
});
