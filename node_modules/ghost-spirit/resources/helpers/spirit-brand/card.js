'use strict';

const hbs = require('express-hbs');
const cardStyles = require('./styles/card');

hbs.registerHelper('gh-card', function (style) {
    let cssClass = cardStyles(style);

    return new hbs.handlebars.SafeString(cssClass);
});
