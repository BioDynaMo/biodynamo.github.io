'use strict';

const hbs = require('express-hbs');
const textStyles = require('./styles/ui-text');

hbs.registerHelper('ui-text', function (style) {
    let cssClass = textStyles(style);

    return new hbs.handlebars.SafeString(cssClass);
});
