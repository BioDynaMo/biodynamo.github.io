'use strict';

const hbs = require('express-hbs');
const kgStyles = require('./styles/koenig');

hbs.registerHelper('kg-style', function (style, options) {
    let cssClass = kgStyles(style, options);

    return new hbs.handlebars.SafeString(cssClass);
});
